name: Ping Replit Bot

on:
  schedule:
    - cron: '*/5 * * * *'  # ping tiap 5 menit
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl to ping bot
        run: curl -s -o /dev/null https://ae2c0229-d320-41fc-8264-d0104845b7dd-00-224lv92mmbwl1.pike.replit.dev:3000/
